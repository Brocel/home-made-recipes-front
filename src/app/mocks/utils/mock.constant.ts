export const MOCK_CURRENT_USER_ID = '2'; // Change to '1' when admin test is needed

export const latency = 300;

export const TOKEN_EXPIRATION_TIME_S = 3600; // Change fake token expiration time when needed
